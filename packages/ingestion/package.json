{
  "name": "@morpho-risk-tooling/ingestion",
  "version": "0.0.1",
  "private": true,
  "description": "Data ingestion (Dune/subgraph/GraphQL) for the Morpho Risk Tooling. Skeleton — no logic at this stage.",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "test": "echo \"(placeholder) no tests at this stage — pre-build validation\" && exit 0"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
